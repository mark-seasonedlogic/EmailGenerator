<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EmailGenerator.Views.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:webview2="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Subject -->
            <RowDefinition Height="Auto"/>
            <!-- To -->
            <RowDefinition Height="Auto"/>
            <!-- CC -->
            <RowDefinition Height="Auto"/>
            <!-- BCC -->
            <RowDefinition Height="*"/>
            <!-- WebView -->
            <RowDefinition Height="Auto"/>
            <!-- Save Button -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Subject Line:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{x:Bind ViewModel.SubjectLine, Mode=TwoWay}" Grid.Row="0" Grid.Column="1"/>

        <TextBlock Text="To Recipients:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{x:Bind ViewModel.StaticToRecipients, Mode=TwoWay}" Grid.Row="1" Grid.Column="1"/>

        <TextBlock Text="CC Recipients:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{x:Bind ViewModel.StaticCcRecipients, Mode=TwoWay}" Grid.Row="2" Grid.Column="1"/>

        <TextBlock Text="BCC Recipients:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{x:Bind ViewModel.StaticBccRecipients, Mode=TwoWay}" Grid.Row="3" Grid.Column="1"/>

        <WebView2 x:Name="EmailEditor"
              Grid.Row="4"
              Grid.ColumnSpan="2"
              Margin="0"
              HorizontalAlignment="Stretch"/>

        <Button Content="Save Settings"
            Click="OnSaveClicked"
            Grid.Row="5"
            Grid.ColumnSpan="2"
            HorizontalAlignment="Right"
            Margin="0,10,0,0"/>
    </Grid>


</Page>
